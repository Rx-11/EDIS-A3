apiVersion: v1
kind: Service
metadata:
  name: crm-service
  namespace: bookstore-ns
spec:
  selector:
    app: crm-service
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
  type: ClusterIP