apiVersion: apps/v1
kind: Deployment
metadata:
  name: customer-service
  
  labels:
    app: customer-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: customer-service
  template:
    metadata:
      labels:
        app: customer-service
    spec:
      containers:
      - name: customer-service
        image: 813170851015.dkr.ecr.us-east-1.amazonaws.com/customer-service:latest # Replace with your image repository and tag
        imagePullPolicy: Always
        env:
        - name: DB_USER
          value: "user"
        - name: DB_PASSWORD
          value: "password"
        - name: DB_HOST
          value: "bookstore-db-dev.cluster-cndk3sokkeyc.us-east-1.rds.amazonaws.com"
        - name: DB_PORT
          value: "3306"
        - name: DB_NAME
          value: "bookstoredb"
        - name: DB_MIGRATE
          value: "true"
        - name: KAFKA_BROKERS
          value: "kafka:9092"
        - name: KAFKA_TOPIC
          value: "vkannawa.customer.evt"
        ports:
        - containerPort: 3000
        livenessProbe:
          httpGet:
            path: /status
            port: 3000
          initialDelaySeconds: 3
          periodSeconds: 10
