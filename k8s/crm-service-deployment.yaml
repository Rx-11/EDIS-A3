apiVersion: apps/v1
kind: Deployment
metadata:
  name: crm-service
  
  labels:
    app: crm-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: crm-service
  template:
    metadata:
      labels:
        app: crm-service
    spec:
      containers:
      - name: crm-service
        image: 813170851015.dkr.ecr.us-east-1.amazonaws.com/crm-service:latest # Replace with your image repository and tag
        imagePullPolicy: Always
        env:
        - name: KAFKA_BROKER
          value: "kafka:9092"
        - name: KAFKA_TOPIC
          value: "vkannawa.customer.evt"
        - name: SMTP_EMAIL
          value: "<your-email>@gmail.com"
        - name: SMTP_PASSWORD
          value: "<your-app-password>"
        ports:
        - containerPort: 8080
