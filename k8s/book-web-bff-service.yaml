apiVersion: v1
kind: Service
metadata:
  name: book-web-bff
  namespace: bookstore-ns
spec:
  selector:
    app: book-web-bff
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
  type: ClusterIP